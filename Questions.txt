!-- Questions to Submit in today's file first-lastname.txt --> <!--Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes.     1. What is Semantic HTML? Marking up information used for a website    2. What is HTML used for? The “foundation” of a website    3. What is an attribute and where do we put it? Words inside a tag to define that element     4. What is the h1 tag used for? How many times should I use it on a page? Header, only 1 time    5. Name two tags that have required attributes <input>, <img>    6. What do we put in the head of our HTML document? The header or what the document is going to be mainly about     7. What is an id? Defines a certain element in HTML    8. What elements can I add an id to? Style, head, body    9. How many times can I use the same id on a page? once    10. What is a class? Defining a class type in a style sheet    11. What elements can I add a class to? Section, div    12. How many times can I use the same class on a page? once    13. How do I get my link to open in a new tab? copy and paste it?    14. What is the alt attribute in the image tag used for? Specify alternative text    15. How do I reference an id? “#”    16. What is the difference between a section and a div <section> defines sections (i.e chapters,footers. <div> division, block elements together for CSS    17. What is CSS used for? The “styling” of a website    18. How to we select an element? Example - every h2 on the page    19. What is the difference between a class and an id? - Give me an example of when I might use each one ID used to identify one element, class and identify more than one.     20. How do we select classes in CSS?putting a “.” In front of it    21. How do we select a p element with a single class of “human””?    22. What is a parent child selector? When would this be useful? Selects all elements that are a direct child of a specified element     23. How do you select all links within a div with the class of sidebar? A;Link{}    24. What is a pseudo selector? To define a special state of an element     25. What do we use the change the spacing between lines? Line-height: 1.0;    26. What do we use to change the spacing between letters? Line-spacing: 1.0;    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? Text-transormation: capitalize;    28. How do I add a 1px border around my div that is dotted and black?    29. How do I select everything on the page? “*”    30. How do I write a comment in CSS? /* comment comment */    31. How do I find out what file I am in, when I am using the command line? pwd    32. Using the command line - how do I see a list of files/folders in my current folder? CD    33. How do I remove a file via the command line? Why do I have to be careful with this? Git.rm you can remove necessary  files if you aren’t careful    34. Why should I use version control? So that you can keep various versions of your project in case you need to revert back to one later    35. How often should I commit to github? Whenever you are ready to upload a new verison of your project    36. What is the command we would use to push our repo up to github? Git push –m”filename”    37. Walk me through Lambda's git flow.